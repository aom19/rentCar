class User {
  constructor(id, email, password, firstName, lastName, phoneNumber, address) {
    this.id = id;
    this.password = password;
    this.email = email;
    this.firstName = firstName;
    this.lastName = lastName;
    this.phoneNumber = phoneNumber;
    this.address = address;
  }
}

export default User;

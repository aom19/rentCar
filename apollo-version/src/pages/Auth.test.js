// import { render } from "@testing-library/react";

// import AuthPage from "./Auth";

// test("on initial rendeer , the sign in button is disabled", async() => {
//   render(<AuthPage />);

//   expect(await screen.findByRole("button", { name: /Submit/i })).toBeEnabled();
// });
